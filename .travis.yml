language: go
services:
  - postgresql
before_script:
  - psql -c 'create database postgres;' -U postgres
env:
  global:
    - APP_DB_USERNAME=postgres
    - APP_DB_PASSWORD=
    - APP_DB_NAME=postgres